{
  "name": "@peaceos/examples",
  "version": "0.1.0",
  "private": true,
  "description": "Sample .vep packages and test vectors (valid and tampered), generated by generate.ts.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "generate": "pnpm run build && node dist/generate.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@peaceos/core": "workspace:*"
  }
}
