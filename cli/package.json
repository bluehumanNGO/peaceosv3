{
  "name": "@peaceos/cli",
  "version": "0.1.0",
  "private": true,
  "description": "Node CLI wrapping @peaceos/core (create, check).",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "peaceos-verify": "./dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@peaceos/core": "workspace:*"
  }
}
